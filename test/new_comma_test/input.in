extern int printf(string fmt, ...);

def int plus(int a, int b) {
  return a -= b, printf("now a is %d.\n", a), a + 2*b;
}

def int main() {
  int a = 3, b = 4;
  return 0*printf("plus returned %d.\n", plus((b, a), b));
}
